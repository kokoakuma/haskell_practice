{
    "image": "haskell:latest",
    "mounts": [
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/github/haskell_practice,target=/workspace,type=bind"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "haskell.haskell"
            ]
        }
    }
}